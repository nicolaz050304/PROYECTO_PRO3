====================================================================
BUNKI / FIXSOFT — ESTADO DEL PROYECTO Y PENDIENTES
(actualizado tras: filtrado por usuario, crear/confirmar reserva,
 menú por rol, hashing + login solo-hash, y auth por cookie)
====================================================================

## ✅ LO QUE YA FUNCIONA (end-to-end: frontend -> REST -> MySQL en RDS AWS)
- LOGIN + AUTH (completo):
  * Login real contra UsuarioRS/login (valida correo + contraseña contra la BD).
  * Contraseñas con HASH BCrypt: insertar() hashea, todas las de la RDS migradas, y autenticar()
    es SOLO-HASH (se quitó la comparación en texto plano). Verificado: contraseña errada -> 401.
  * Sesión por COOKIE de ASP.NET Core (ClaimsPrincipal + SignInAsync vía /auth/login y /auth/logout).
    Sobrevive a F5. AppState se hidrata desde los claims, así que EsAdmin()/EsAnfitrion()/EstaAutenticado
    siguen funcionando. Login.razor es una página estática que postea al endpoint.
- ALOJAMIENTO: lectura (Explorar, detalle, mapa) + escritura (crear).
- RESERVA:
  * Lectura FILTRADA por usuario: Mis Reservas por invitado (ReservaRS/usuario/{id}); Reservas
    Recibidas por anfitrión (ReservaRS/anfitrion/{id}). Lista vacía es válida (no cae al mock).
  * Escritura: la reserva se persiste al COMPLETAR el pago en /pagos -> crea (POST) si es nueva, o
    confirma la existente (PUT) si viene reservaId (evita duplicados al pagar una pendiente).
- RESEÑA: módulo completo (crear, leer en detalle, vista anfitrión /mis-resenas, moderación admin).
- MENÚ POR ROL: la sección "Como anfitrión" del menú solo la ve EsAnfitrion(); el bloque Admin solo EsAdmin().

## ❌ PENDIENTES
- PANEL DE ANFITRIÓN: métricas (ingresos, ocupación) son mock; conectar a datos reales.
- ADMIN - USUARIOS: la lista es real, pero editar/crear/eliminar son mock (falta POST/PUT/DELETE en
  UsuarioRS + verificar UsuarioBL).
- CREAR ALOJAMIENTO (mejoras): no captura campos de subclase ni coordenadas (lat/long en 0 -> sin pin).
- EDITAR/CANCELAR RESERVA; EDITAR/ELIMINAR ALOJAMIENTO (PUT/DELETE existen, falta UI).
- IMÁGENES de alojamientos (columna imagen_url -> modelo/DAO/DTO/mapper).
- Mensaje de huésped en Reservas Recibidas: el DTO no trae nombre del huésped (muestra el del anfitrión);
  agregar huespedNombre al ReservaDTO + mapearlo.
- PAGO (transacción) y NOTIFICACIÓN: hoy mock. Baja prioridad.

## ⚙️ OPTIMIZACIÓN (pendiente)
- HIKARI (pool de conexiones): DBManager.getConnection() abre conexión nueva cada vez -> Reserva abre
  ~13 por petición (~8s). Pool -> <1s. Toca SOLO el DBManager; los DAO no cambian. Con commit de respaldo.
- REDIS (caché): DESPUÉS de Hikari. Cachear lookups de Reserva y listados.
- Demo: "calentar" el servidor antes de presentar + timeout 20s en el frontend.
- NO hacer (sobre-ingeniería): balanceador, microservicios, colas, CQRS.

## 🔒 SEGURIDAD (antes de publicar)
- Regenerar API key de Stadia (en wwwroot/js/mapa.js).
- docs/usuarios.txt y db.properties tienen credenciales: OK en repo PRIVADO; quitarlas del historial si
  se hace público. (Nota: las contraseñas en usuarios.txt siguen siendo demo1234 — correcto, ahora se
  guardan hasheadas.)
- HASHING de contraseñas: ✅ hecho.

## 🎯 ORDEN SUGERIDO PARA RETOMAR
1. Hikari (optimización; mejor esfuerzo/impacto, arregla la lentitud de ~8s).
2. Panel de anfitrión con datos reales (más visible para el jurado).
3. Redis (después de Hikari).
4. CRUD de usuarios del admin (necesita endpoints en UsuarioRS).
5. Imágenes, editar/cancelar reserva, mejoras de crear alojamiento.
6. Pago, notificaciones.
====================================================================
